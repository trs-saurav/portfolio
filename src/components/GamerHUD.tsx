'use client';

// GamerHUD intentionally left minimal — SYNC_LEVEL lives in the navbar only.
// This file kept to avoid breaking imports elsewhere.
export default function GamerHUD() {
  return null;
}
